import React, { useRef, useState } from 'react'
import {
  Text,
  View,
  SafeAreaView,
  StyleSheet,
  Dimensions,
  ActivityIndicator,
  Image,
} from 'react-native'

function AppDescriptionScreen(props) {
  return <Text style={{ color: 'red' }}>lol</Text>
}

export default AppDescriptionScreen
